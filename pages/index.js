import Login from '../src/views/Login';
import SiteContainer from '../src/sections/SiteContainer';

const Redirect = (props) => {

  return <SiteContainer><Login /></SiteContainer>
}

export default Redirect